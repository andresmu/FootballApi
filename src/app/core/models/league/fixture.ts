interface IFixture {
    events: boolean;
    lineups: boolean;
    statistics: boolean;
    players_statistics: boolean;
}

export class Fixture implements IFixture {
    events: boolean;
    lineups: boolean;
    statistics: boolean;
    players_statistics: boolean;
}